# =====================================================
# .env.example —— 复制为 .env 后填入真实值（.env 已在 .gitignore）
# =====================================================

# --- Claude Agent SDK (Roadmap，当前未引用) ---
# 未来用于自动 ingest / 自动 digest 等场景。
# 当前仓库代码不读取这些变量，仅作占位。
#
# 方案 A：Anthropic 官方 API
# ANTHROPIC_AUTH_TOKEN=sk-ant-your_key_here
# ANTHROPIC_BASE_URL=https://api.anthropic.com
# MODEL=claude-sonnet-4-6
#
# 方案 B：DeepSeek 等 Anthropic 兼容接口
# ANTHROPIC_AUTH_TOKEN=your_key_here
# ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
# MODEL=deepseek-v4-flash


# --- Quartz 站点配置 ---
# 被 quartz/quartz.config.ts 读取。
# - SITE_BASE_URL 影响 sitemap / RSS / og:url / canonical 等绝对链接
# - 必须**不带**协议前缀（不要写 https://）
# - 部署时 deploy.sh 会自动 export SITE_BASE_URL=$DEPLOY_DOMAIN，覆盖此处的值
# - 本地 npx quartz build 不设环境变量时，fallback 为 localhost:8080
#
# SITE_BASE_URL=wiki.bytelighting.cn
# SITE_TITLE=LLM Wiki
