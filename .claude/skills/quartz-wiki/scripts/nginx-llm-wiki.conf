# /etc/nginx/conf.d/llm-wiki.conf
#
# llm-wiki 静态站（Quartz 生成）
# 部署目录: /home/www/website/llm-wiki/
#
# 安装步骤:
#   1. 上传到服务器: scp nginx-llm-wiki.conf root@47.96.125.148:/etc/nginx/conf.d/llm-wiki.conf
#   2. 测试配置:     ssh root@47.96.125.148 'nginx -t'
#   3. 重载:         ssh root@47.96.125.148 'systemctl reload nginx'
#   4. 申请 HTTPS:   ssh root@47.96.125.148 'certbot --nginx -d wiki.bytelighting.cn'
#      （certbot 会自动追加 443 server block 并配置 80→443 跳转）

server {
    listen       80;
    listen       [::]:80;
    server_name  wiki.bytelighting.cn;

    root         /home/www/website/llm-wiki;
    index        index.html;

    # 安全头
    add_header X-Content-Type-Options   "nosniff" always;
    add_header X-Frame-Options          "SAMEORIGIN" always;
    add_header Referrer-Policy          "strict-origin-when-cross-origin" always;

    # Quartz 输出 <name>.html 而非目录，URL 自动补 .html
    location / {
        try_files $uri $uri.html $uri/ =404;
    }

    # 静态资源长缓存
    location ~* \.(css|js|woff2?|ttf|otf|svg|png|jpg|jpeg|gif|webp|ico)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # 隐藏 nginx 版本
    server_tokens off;

    # 访问日志
    access_log /var/log/nginx/llm-wiki.access.log main;
    error_log  /var/log/nginx/llm-wiki.error.log warn;
}
